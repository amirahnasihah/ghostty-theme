[install]
# Only install package versions published at least 7 days ago (604800 seconds).
# Mitigates rapid supply-chain attacks on freshly published versions.
minimumReleaseAge = 604800

# Use `bun install --frozen-lockfile` in CI to fail when lockfile is out of date.
# frozenLockfile = true

[install.lockfile]
save = true
